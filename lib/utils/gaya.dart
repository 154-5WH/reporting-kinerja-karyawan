import 'package:flutter/material.dart';

class Gaya extends StatelessWidget {
  const Gaya({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

TextStyle GayaText() {
  return TextStyle(
      fontSize: 20, fontWeight: FontWeight.bold, color: Colors.purple[900]);
}
